// API contract type is intentionally published from shared to avoid
// frontend type-checking against backend runtime entrypoints.
// Replace `any` with generated contract types when OpenAPI generation is in place.
export type ApiContract = any;
